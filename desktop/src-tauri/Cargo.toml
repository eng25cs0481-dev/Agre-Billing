[package]
name = "agre-billing"
version = "1.0.0"
description = "Agre Billing — Shop billing and accounting desktop application"
authors = ["Agre"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-updater = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
